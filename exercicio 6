//Leia 2 valores A e B, que correspondem a 2 notas de um aluno. A seguir, calcule a
//média do aluno, sabendo que a nota A tem peso 3.5 e a nota B tem peso 7.5 (A soma
//dos pesos portanto é 11). Assuma que cada nota pode ir de 0 até 10.0.

#include <stdio.h>
#include <stdlib.h>
#include <locale.h>

int main(){
	float A, B, A1, B1, C;
	printf("adicione a primeira nota: ");
	scanf("%f", &A);
	printf("adicione a segunda nota: ");
	scanf("%f", &B);
	
	A1 = A * 3,5;
	B1 = B * 7,5;
	
	C = (A1 + B1)/11;
	
	if(C > 10){
		printf("sua media foi 10.0");
	}
	else
	{
		printf("sua media foi %f", C);
	}
}
